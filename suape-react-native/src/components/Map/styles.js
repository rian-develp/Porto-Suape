import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
      flex: 1,
    },
    map: {
      minWidth: '100%',
      minHeight: '100%',
    },
  });


export default styles;