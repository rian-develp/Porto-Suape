import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    inputSelect: {
        borderRadius: 10,
        width: "80%",
        marginLeft: "auto",
        marginRight: "auto",
        marginTop: 20,
    }
});

export default styles;