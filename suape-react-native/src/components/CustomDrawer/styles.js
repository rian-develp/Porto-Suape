import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  title: {
    padding: 30,
    textAlign: 'center',
    fontSize: 16,
    fontWeight: 'bold',
  },
  welcomeTitle: {
    textAlign: 'center',
    marginBottom: 20,
  },
});

export default styles;
