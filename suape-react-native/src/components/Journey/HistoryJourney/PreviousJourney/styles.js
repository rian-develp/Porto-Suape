import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  flatList: {
    flexGrow: 0,
    marginTop: 20,
  },
  itemText: {
    marginBottom: 20,
    textAlign: 'center',
  },
});

export default styles;
